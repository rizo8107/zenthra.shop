export NODE_OPTIONS="--max-old-space-size=512" && npm install --production --no-fund --no-audit && npm install --no-fund --no-audit --no-save && NODE_ENV=production npx vite build --minify --outDir dist && mkdir -p dist-server && npx tsc -p tsconfig.server.json && mkdir -p dist-server/server dist-server/api dist-server/lib && cp -r src/server dist-server/ && cp -r src/api dist-server/ && cp -r src/lib dist-server/ && npm prune --production 